const reqStr = {
  type: String,
  required: true,
};

const reqNum = {
  type: Number,
  required: true,
};
const reqNumZero = {
  type: Number,
  required: true,
};

module.exports = { reqStr, reqNum, reqNumZero };
